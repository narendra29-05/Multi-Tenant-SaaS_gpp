# Multi-Tenant SaaS Project & Task Management

A production-ready SaaS platform built with the PERN stack (PostgreSQL, Express, React, Node) and Docker.

## Quick Start
To run the entire system (Database, Backend, Frontend) with a single command:
```bash
docker-compose up -d